models:
  - name: int_backfill_anchor
    columns:
      - name: real_estate_equity
        data_tests:
          - accepted_values:
              arguments:
                values: ["Yes", "No", "TBD", "strategy_unresolvable"]
      - name: real_estate_debt
        data_tests:
          - accepted_values:
              arguments:
                values: ["Yes", "No", "TBD", "strategy_unresolvable"]
      - name: infrastructure_equity
        data_tests:
          - accepted_values:
              arguments:
                values: ["Yes", "No", "TBD", "strategy_unresolvable"]
      - name: infrastructure_debt
        data_tests:
          - accepted_values:
              arguments:
                values: ["Yes", "No", "TBD", "strategy_unresolvable"]
  - name: int_pns_deals
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - deal_id
              - reporting_date
