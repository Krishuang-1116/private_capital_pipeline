models:
  - name: stg_pns
    data_tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - deal_id
              - reporting_date
    columns:
      - name: revenue_eur_equiv
        data_tests:
          - not_null
